<div class="page-title max-w-content mx-auto text-center mb-12">
    {% if data.title %}
    <h1 class="text-white">{{ data.title }}</h1>
    {% endif %}
    {% if data.description %}
    <p class="text-lg text-neutral-300">{{ data.description }}</p>
    {% endif %}
</div>